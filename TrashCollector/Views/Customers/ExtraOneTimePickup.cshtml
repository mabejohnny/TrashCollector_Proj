@model TrashCollector.Models.Customer

@{
    ViewData["Title"] = "ExtraOneTimePickup";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Customer Portal</h1>

<h4>When would you like the Schedule your extra pickup?</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ExtraOneTimePickup">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OneTimePickup" class="control-label"></label>
                <input asp-for="OneTimePickup" class="form-control" />
                <span asp-validation-for="OneTimePickup" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to main menu</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
